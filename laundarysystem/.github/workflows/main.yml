on: 
    push:
        branches:
            - main
name: 🚀 Test Deploy
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.wavemenswear.store
        username: washpro@app.washpro.com.np
        password: WashPro@123#
